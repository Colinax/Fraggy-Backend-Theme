$(function () {    var $themeStylesheet = $('link#themeStylesheet');    $('.sidebar .colors a').on('click', function (e) {        e.preventDefault();        var cssFile = $(this).attr('href');        $('body').fadeOut(function () {            var $body = $(this),                    $newThemeStyleSheet = $themeStylesheet                    .clone()                    .attr('href', cssFile);            $themeStylesheet.after($newThemeStyleSheet);            $newThemeStyleSheet.load(function () {                $themeStylesheet.remove();                $themeStylesheet = $newThemeStyleSheet;                $body.fadeIn();            });        });    }).blur();    $(window).on('resize', function () {        var $window = $(this);        if ($window.outerWidth() > 768) {            $('nav.sidebar').height($('body').outerHeight());        } else {            $('nav.sidebar').height('');        }    }).resize();    $('select').select2({        theme: 'bootstrap',        minimumResultsForSearch: 5,        width: '100%',        dropdownAutoWidth: true    }).focus(function () {        $(this).select2('open');    });    $('.datetimepicker').datetimepicker({        locale: 'en',        format: 'D.M.Y HH:mm',        useCurrent: false    });    $.ajax({        dataType: 'json',        url: THEME_URL + '/api/sysinfo.php',        success: function (data) {            $('ul.versions').find('.php').text(data.php.version);        },        error: function (jqXHR, textStatus, errorThrown) {            $('ul.versions').find('.php').parent().remove();        }    });});